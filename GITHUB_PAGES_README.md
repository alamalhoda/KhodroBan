# 🚀 راهنمای تنظیم GitHub Pages

این سند راهنمای کامل برای تنظیم و اجرای پروژه روی GitHub Pages است.

---

## 📋 مزایای GitHub Pages

- ✅ **رایگان کامل** - بدون هیچ هزینه‌ای
- ✅ **CDN جهانی** - سرعت بالا در سراسر جهان
- ✅ **امنیت بالا** - هاست شده توسط GitHub
- ✅ **یکپارچگی کامل** با GitHub
- ✅ **بدون محدودیت build** (برخلاف Netlify رایگان)

---

## ⚙️ تنظیمات خودکار

پروژه از قبل برای GitHub Pages آماده شده است! فقط کافی است:

### مرحله ۱: فعال کردن GitHub Pages

1. **به Repository خود بروید**
2. **روی Settings کلیک کنید**
3. **در منوی سمت چپ، Pages را انتخاب کنید**
4. **در بخش Source، گزینه "GitHub Actions" را انتخاب کنید**

### مرحله ۲: تنظیمات Build

**هیچ کاری نیاز نیست!** فایل workflow از قبل تنظیم شده:

```yaml
# .github/workflows/deploy-github-pages.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'

# Build و deploy خودکار
```

### مرحله ۳: مشاهده نتیجه

بعد از push به main:
- ✅ GitHub Actions اجرا می‌شود
- ✅ پروژه build می‌شود
- ✅ روی GitHub Pages deploy می‌شود
- ✅ URL شما آماده است!

---

## 🔗 URL نهایی

URL پروژه شما خواهد بود:
```
https://YOUR_USERNAME.github.io/OilChenger
```

**مثال:**
```
https://alamalhoda.github.io/OilChenger
```

---

## 🎯 ویژگی‌های خاص GitHub Pages

### URL Redirect خودکار

پروژه به صورت هوشمند URL مناسب را برای OAuth redirect تشخیص می‌دهد:

```javascript
// config.ts
case 'github-pages':
  // username.github.io/repository-name
  const repoName = pathname.split('/')[1] || 'OilChenger';
  return `https://${hostname}/${repoName}`;
```

### Static Site Generation

از `adapter-static` استفاده می‌کند که برای GitHub Pages کاملاً بهینه است.

---

## 🔧 عیب‌یابی

### مشکل: صفحه سفید نمایش داده می‌شود

**علت:** Base path تنظیم نشده

**راه‌حل:** مطمئن شوید که repository name درست است. اگر repository شما `OilChenger` نام دارد، URL باید `https://username.github.io/OilChenger` باشد.

### مشکل: ۴۰۴ Not Found

**علت:** فایل `index.html` پیدا نمی‌شود

**راه‌حل:** مطمئن شوید که workflow با موفقیت اجرا شده و فایل‌های build در branch `gh-pages` قرار گرفته‌اند.

### مشکل: OAuth کار نمی‌کند

**علت:** URL redirect اشتباه

**راه‌حل:** مطمئن شوید که `config.redirectBaseUrl` URL درست را برمی‌گرداند.

---

## 📊 مقایسه پلتفرم‌ها

| ویژگی | GitHub Pages | Deno Deploy | Netlify |
|-------|-------------|-------------|---------|
| **هزینه** | رایگان | رایگان | رایگان محدود |
| **سرعت** | عالی (CDN) | عالی | عالی |
| **امنیت** | عالی | عالی | عالی |
| **سهولت تنظیم** | متوسط | متوسط | آسان |
| **Serverless** | ❌ | ✅ | ✅ |
| **Custom Domain** | ✅ | ✅ | ✅ |

---

## 🎉 نتیجه

حالا پروژه شما روی **۳ پلتفرم مختلف** اجرا می‌شود:

1. **Deno Deploy** - برای عملکرد serverless
2. **Netlify** - برای backup و تست
3. **GitHub Pages** - برای دسترسی رایگان و سریع

هر پلتفرم مستقل است و اگر یکی از آنها مشکل داشته باشد، بقیه همچنان کار می‌کنند! 🚀
